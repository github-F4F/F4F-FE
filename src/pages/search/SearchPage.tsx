import UserInfo from './component/UserInfo';

const SearchPage = () => {
  return (
    <div
      css={{
        display: 'flex',
        justifyContent: 'center',
      }}
    >
      <UserInfo />
    </div>
  );
};

export default SearchPage;
